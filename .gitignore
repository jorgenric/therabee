*.hprof
*.iml
.gradle
/local.properties
/.idea
.DS_Store

# Build output — matches app/build/, build/, any nested build/ directory
build/

/captures
.externalNativeBuild
.cxx
local.properties

# App build outputs
*.apk
*.aab
*.ap_
*.dex

# Keystore / signing — never commit these
*.jks
*.keystore
keystore.properties

# Kotlin incremental compilation cache
*.tab
*.tab.values.at
*.tab.keystream
*.tab.keystream.len
*.tab.len
*.tab_i
*.bin
*.at

# Profiling and benchmark artifacts
*.trace

# macOS metadata
__MACOSX/

# Gradle wrapper jar — must be committed for reproducible builds
# Do NOT add: gradle/wrapper/gradle-wrapper.jar
