<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exposes getExternalFilesDir(null)/backups/ for share-sheet intents -->
    <external-files-path
        name="backups"
        path="backups/" />
    <!-- Exposes cacheDir for temp export files -->
    <cache-path
        name="cache"
        path="." />
</paths>
